# Runtime status

Stage 1 is a local-first ingest and metadata-probing foundation. It stops at
`READY_FOR_TRANSCRIPTION`; no transcription, automatic clip selection, render,
or social publishing feature is included.

See the task report for the latest local verification evidence. Copy
`.env.example` to `.env` before starting Compose.
